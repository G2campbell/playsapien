node_modules/
dist/
build/out/
# the one-file builds each game emits beside its dist/ (build.py --single)
games/*/preview/
*.log
.DS_Store
desktop.ini
.wrangler/
.dev.vars
.env
# Build inputs too large for git. Both are public downloads; see the games'
# READMEs for where to get them.
games/sojourner/data/
*.tif
*.zip
*.tar.gz
__pycache__/
*.pyc
